#include <miopen/db.hpp>
#include <cstdlib>

namespace miopen {

std::string GetDbPath()
{
    auto p = std::getenv("MIOPEN_DB_PATH");
    if (p == nullptr) return "${MIOPEN_DB_PATH}";
    else return p;
}

} // namespace miopen

